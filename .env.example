# SQLAlchemy async URL for PostgreSQL 16
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/policies

# Logging
LOG_LEVEL=INFO

# Outbox worker
WORKER_POLL_INTERVAL_SECONDS=1.0
WORKER_BATCH_SIZE=10
WORKER_MAX_RETRIES=5
WORKER_BACKOFF_BASE_SECONDS=1.0
